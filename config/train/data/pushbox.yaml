dataset:
  num_steps: ${eval:'${num_preds} + ${history_size}'}
  frameskip: 1
  name: ${oc.env:PUSHBOX_DATASET,data/pushbox_keyboard_20260713.h5}
  keys_to_load:
    - pixels
    - action
  keys_to_cache:
    - action
