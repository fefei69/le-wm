# @package _global_
# Local launcher configuration (no SLURM)

defaults:
  - override /hydra/launcher: basic

wandb:
  enabled: True
  config:
    entity: cw5167-nyu
    project: le-wm
    name: ${output_model_name}
    id: ${subdir}
    resume: allow
    log_model: False